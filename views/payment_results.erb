<!DOCTYPE html>
  <header>
  </header>
  <body>
    <form>
      <h1>Payment Results</h1>
      <dl>
        <dt>APR</dt>
        <dd>
          <%= params.fetch("users_number_apr").to_i.to_fs(:percentage, { :precision => 4 }) %>
        </dd>

        <dt>Number of years</dt>
        <dd>
          <%= params.fetch("users_number_years").to_i %>
        </dd>

        <dt>Principal</dt>
        <dd>
          <%= params.fetch("users_number_principal").to_i.to_fs(:currency)%>
        </dd>

        <dt>Payment</dt>
        <dd>
          <%=  %>
        </dd>
      </dl>
      <a href="/payment/new">
        Calculate another payment
      </a>
    </form>
  </body>
</html>
